// Twitter summary_large_image cards use the same design as OG.
// Re-export everything from opengraph-image so there's a single source of truth.
export { default, alt, size, contentType } from "./opengraph-image";
